name: Build Vue
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build 
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: 'hglong16'
        reponame: 'pokemon-flipcard'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged
